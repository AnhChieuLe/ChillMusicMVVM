<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="currentPlayerViewModel"
            type="com.example.chillmusic.object.CurrentPlayer"/>
        <variable
            name="songViewModel"
            type="com.example.chillmusic.adapter.SelectionSongAdapter.SelectableSong" />
    </data>

    <androidx.cardview.widget.CardView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/card_song"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="3dp"
        app:cardBackgroundColor="@android:color/transparent"
        app:cardCornerRadius="10dp"
        app:cardElevation="0dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@{currentPlayerViewModel.style.itemBackGround}"

            android:paddingHorizontal="10dp"
            android:paddingVertical="5dp">

            <LinearLayout
                android:id="@+id/ln_song"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:layout_toStartOf="@id/ic_check"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <androidx.cardview.widget.CardView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:cardBackgroundColor="@android:color/transparent"
                    app:cardCornerRadius="7dp"
                    app:cardElevation="0dp">

                    <ImageView
                        android:id="@+id/img_album_art"
                        android:layout_width="45dp"
                        android:layout_height="45dp"
                        android:scaleType="centerCrop"
                        android:bitmap="@{songViewModel.song.smallImage}"
                        android:contentDescription="@{currentPlayerViewModel.song.title}" />
                </androidx.cardview.widget.CardView>

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="15dp"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/tv_title"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:maxLines="1"
                        android:text="@{songViewModel.song.title}"
                        android:textColor="@{songViewModel.equals(currentPlayerViewModel.song) ? currentPlayerViewModel.style.contentColor : currentPlayerViewModel.style.titleColor}"
                        android:textSize="16sp"
                        android:textStyle="bold" />

                    <TextView
                        android:id="@+id/tv_artist"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:maxLines="1"
                        android:text="@{songViewModel.song.artist}"
                        android:textColor="@{songViewModel.equals(currentPlayerViewModel.song) ? currentPlayerViewModel.style.contentColor : currentPlayerViewModel.style.titleColor}"
                        android:textSize="12sp" />

                    <TextView
                        android:id="@+id/tv_album"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:maxLines="1"
                        android:text="@{songViewModel.song.album}"
                        android:textColor="@{songViewModel.equals(currentPlayerViewModel.song) ? currentPlayerViewModel.style.contentColor : currentPlayerViewModel.style.titleColor}"
                        android:textSize="12sp" />
                </LinearLayout>
            </LinearLayout>

            <CheckBox
                android:id="@+id/ic_check"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:layout_marginEnd="0dp"
                android:checked="@={songViewModel.selected}"
                android:buttonTint="@{currentPlayerViewModel.style.contentColor}">
            </CheckBox>
        </RelativeLayout>
    </androidx.cardview.widget.CardView>
</layout>