<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="currentPlayerViewModel"
            type="com.example.chillmusic.viewmodel.CurrentPlayerViewModel"/>
        <variable
            name="songViewModel"
            type="com.example.chillmusic.model.Song" />
    </data>

    <androidx.cardview.widget.CardView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/card_song"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="3dp"
        app:cardBackgroundColor="@android:color/transparent"
        app:cardCornerRadius="10dp"
        app:cardElevation="0dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@{currentPlayerViewModel.style.itemBackGround}"

            android:paddingHorizontal="10dp"
            android:paddingVertical="5dp">

            <LinearLayout
                android:id="@+id/ln_song"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:layout_toStartOf="@id/ln_icon"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <androidx.cardview.widget.CardView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:cardBackgroundColor="@android:color/transparent"
                    app:cardCornerRadius="7dp"
                    app:cardElevation="0dp">

                    <ImageView
                        android:id="@+id/img_album_art"
                        android:layout_width="45dp"
                        android:layout_height="45dp"
                        android:scaleType="centerCrop"
                        android:bitmap="@{songViewModel.smallImage}"
                        android:contentDescription="@{currentPlayerViewModel.song.title}" />
                </androidx.cardview.widget.CardView>

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="15dp"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/tv_title"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:maxLines="1"
                        android:text="@{songViewModel.title}"
                        android:textColor="@{songViewModel.equals(currentPlayerViewModel.song) ? currentPlayerViewModel.style.contentColor : currentPlayerViewModel.style.titleColor}"
                        android:textSize="16sp"
                        android:textStyle="bold" />

                    <TextView
                        android:id="@+id/tv_artist"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:maxLines="1"
                        android:text="@{songViewModel.artist}"
                        android:textColor="@{songViewModel.equals(currentPlayerViewModel.song) ? currentPlayerViewModel.style.contentColor : currentPlayerViewModel.style.titleColor}"
                        android:textSize="12sp" />

                    <TextView
                        android:id="@+id/tv_album"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:maxLines="1"
                        android:text="@{songViewModel.album}"
                        android:textColor="@{songViewModel.equals(currentPlayerViewModel.song) ? currentPlayerViewModel.style.contentColor : currentPlayerViewModel.style.titleColor}"
                        android:textSize="12sp" />
                </LinearLayout>
            </LinearLayout>

            <LinearLayout
                android:id="@+id/ln_icon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:layout_marginEnd="0dp"
                android:orientation="horizontal">

                <androidx.appcompat.widget.AppCompatImageView
                    android:id="@+id/img_more"
                    android:layout_width="25dp"
                    android:layout_height="25dp"
                    android:alpha="0.5"
                    android:src="@drawable/more"
                    android:tint="@{songViewModel.equals(currentPlayerViewModel.song)? currentPlayerViewModel.style.contentColor : currentPlayerViewModel.style.titleColor}" />
            </LinearLayout>
        </RelativeLayout>
    </androidx.cardview.widget.CardView>
</layout>